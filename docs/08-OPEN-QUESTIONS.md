# 未决事项 / 待补充

整理打包时尚未解决的事项。

---

## 阻塞性（影响开工）

### 1. 服务器 SSH 接入

- 现状：`35.173.251.33:22` 超时（防火墙挡了 SDK 出口 IP）
- HTTP 80 端口能访问，返回 403（说明已有服务，需确认是什么）
- **需要你做**：
  - AWS Security Group 把 22 端口开给 `0.0.0.0/0`（部署完收回）
  - 或告诉我服务器实际 SSH 端口
  - 或确认服务器是否还存活

### 2. GitHub 仓库

- 沙箱环境无 `gh` CLI、无 GitHub token
- 能 clone 公开 repo 但无法 push / 创建 repo
- **需要你做**：
  - 提供 GitHub 用户名 + Personal Access Token (`repo` 权限)
  - 或自己创建 repo 后告诉我地址，我推送时再要 token

### 3. 域名

- 部署需要域名做 SSL（Let's Encrypt 不签 IP 证书）
- **需要你做**：
  - 确认有没有域名
  - 若有，告诉我域名 + 把 A 记录指到 35.173.251.33

### 4. 邮件服务

- 邮箱注册需发验证邮件、密码重置邮件
- **需要你做**：
  - 选邮件服务：Resend / Postmark / SendGrid / 自己 SMTP
  - 提供 API key 或 SMTP 凭据

---

## 非阻塞但需要早决策

### 5. 80 端口现有服务

- 服务器现在 80 端口已有响应（403）
- 是已有项目？默认 Nginx？需要我处理掉？

### 6. 应用名 / 品牌名

- 仓库名建议：`reminder-app`，你想换吗？
- App 显示名 / 域名？

### 7. 文件存储（提醒附图）

- v1 选择：
  - a) 本地磁盘（简单，但要管理空间和备份）
  - b) S3 兼容服务（AWS S3、Cloudflare R2、阿里 OSS……）
- 你的偏好？

### 8. 拍拍音效 / 完成音效

- 设计稿提到"音效暗示"
- 需要选/做音效素材吗？v1 可以先静默，v2 加

### 9. 移动端 vs 桌面端范围

- 设计稿桌面端是低保真 wireframe，移动端是 hi-fi
- v1 是只做移动端响应式（桌面打开也能用，但样式按移动端来），还是两端都精细化？

### 10. i18n

- 现在所有文案都是中文
- 需要设计 i18n 框架吗？还是 v1 中文 only？

---

## 后续可以再讨论

### 11. 数据导入/导出

- 用户能导出自己的数据吗？（GDPR 友好）
- 能从其他 reminder app 导入吗？

### 12. 群组上限

- 默认 50 人/群，是否合适？

### 13. 删除账号的灰度

- 软删除 vs 硬删除？多久才物理删除？

### 14. 监控/告警

- v1 用 PM2 + 简单 cron 健康检查
- 是否需要接入 Sentry 这类错误追踪？

### 15. 内容审核 AI

- 举报队列纯人工，未来要接入 AI 自动初筛吗？
</content>
</invoke>